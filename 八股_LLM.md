# LLM

+++

1. 请详细解释一下 Transformer 模型中的自注意力机制是如何工作的？它**为什么比 RNN 更适合处理长序列？**

   * QKV向量，计算当前token的q对之前所有token的k做点积，计算注意力分数，除以根号dk缩放防止梯度消失，再经过softmax得到注意力权重，然后对v做加权求和得到输出

   * RNN处理序列是逐步递进的，每一步的隐藏态依赖于前一个隐藏态；反向传播沿着时间步，序列越长梯度容易消失，无法关联长距离依赖；无法并行计算，训练慢

2. 什么是位置编码？在 Transformer 中，为什么它是必需的？**请列举至少两种实现方式。**

   * 位置编码是为了解决 Transformer 自注意力机制的**置换不变性**问题。自注意力本质上是 **token 集合**操作，对**输入词的顺序完全不敏感**。"猫追狗"和"狗追猫"在没有位置信息的情况下会得到完全相同的表示

3. 请你详细介绍RoPE，**对比绝对位置编码它的优劣势分别是什么？**

   * PoPE 的核心思想是：不把位置信息加到词向量上，而是在计算注意力时，对 Q 和 K 向量按照位置施加一个旋转矩阵，使得 Q 和 K 的内积天然只与两个位置的相对距离有关，而不依赖绝对位置。
   * 实现上，RoPE 将 $d $ 维向量两两分组，每组做二维旋转，不同组使用不同频率的旋转角 $\theta_i = 10000^{-2i/d} $，最终可以用两次逐元素乘法加一次加法高效实现，无需构造大矩阵。
   * 优势：
     1. 天然具备相对位置感知，注意力分数直接反映相对距离。绝对位置编码需要模型自己从数据中学习这种关系
     2. 具备远程衰减性，位置差越大，QK点积中位置关系就越弱，**符合自然语言对于相邻词越相关的直觉**
     3. 外推能力强，RoPE 配合 NTK Scaling、YaRN 等技术可以有效扩展上下文窗口
   * 绝对位置编码，直接与token embedding相加，得到的是绝对位置信息而不是 token 之间相对位置信息；长度外推能力差。

4. 你知道MHA，GQA，MQA，MLA的区别吗？详细解释一下。

   * MHA：每个 head 都有**独立的 Q、K、V 投影**

     1. 表达能力强，可以关注到更多的语义信息，每个 head 独立学习不同的注意力模式
     2. KV Cache 最大：$h $ 个 head 各自缓存 K 和 V

   * MQA：所有 Q head **共享同一组 K 和 V**，只有 Q 是多头的。K、V 只有一个投影矩阵，所有 head 共用。

     1. KV Cache 压缩 head 倍，显存开销减少
     2. 推理速度显著加快
     3. 表达能力减弱

   * GQA：MHA 和 MQA 的折中：将 Q head **分成 G 组**，每组共享一对 K、V。

     1. KV Cache 压缩 h/G 倍。适度压缩，表达能力接近MHA

   * MLA：自己讲

5. 请比较一下几种常见的 LLM 架构，例如 Encoder-Only, Decoder-Only, 和 Encoder-Decoder，并说明它们各自最擅长的任务类型。

   双向 Self-Attention（每个 token 可以看到序列中所有其他 token）随机遮盖部分 token，让模型预测被遮盖的词

6. 什么是Scaling Laws？它揭示了模型性能、计算量和数据量之间的什么关系？这对LLM的研发有什么指导意义？

   * 描述了语言模型的**损失（Loss）与模型参数量 $N $、训练数据量 $D $、计算量 $C $ 之间的幂律关系（Power Law）** ：

     * 在预训练阶段，Chinchilla Scaling Law 认为模型参数量N应该跟训练token数D等比例扩展

     * 但是目前一般业界都会“over-train”，原因就是推理成本（inference cost）在实际部署中占大头，训练一个更小但训练更充分的模型，推理时更便宜

     * 最近的趋势是**inference-time scaling**（如OpenAI o1），通过在推理时投入更多计算（更长的chain-of-thought）来提升性能，这可以看作scaling law在另一个维度的延伸

       DeepSeek等团队也在探索更精细的scaling law，考虑数据质量、数据配比（代码vs文本vs数学）等因素对最优比例的影响

   

7. 在LLM的推理阶段，有哪些常见的**解码策略**？请解释 Greedy Search, Beam Search, Top-K Sampling 和 Nucleus Sampling (Top-P) 的原理和优缺点。

   

8. 什么是词元化**（**Tokenization）？请比较一下 BPE 和 WordPiece 这两种主流的子词切分算法。

   * **词元化**是将原始文本切分成模型可以处理的**最小单元Token**的过程，然后将每个 token **映射到词表 ID** 的过程。
     * 切分粒度：**词**（词表太大；OOV；新词处理差）、**字符**（序列太长，计算量爆炸；单字符语义信息太少；词表小但表达效率低）、**子词**（高频词保留，低频词拆成更小的子词单元，兼顾词表大小和语义完整性）

   * BPE：初始化词表为所有单字符，统计语料中所有**相邻 token 对**的频率，将频率最高的 token 对合并为一个新 token，加入词表，直到词表达到目标大小

     （常见词会被合并成完整词元，罕见词会被拆成子词，最坏退化到字符，没有 OOV 问题）

   * wordPiece：初始化词表为所有单字符，每次选择合并后**能最大化语言模型似然**的 token 对：

   * Unigramn：方向相反，自顶向下从大词表开始删减，用语言模型概率决定保留哪些词元

9. **L1和L2正则化分别是什么，什么场景适合使用呢？**

   L1 和 L2 正则化，本质上都是在原始损失函数后面加一个“参数惩罚项”，目的是限制模型复杂度，防止过拟合。

   L1 正则化是在损失函数后面加上**参数绝对值之和**，抑制过拟合，让参数变得稀疏；

   L2 正则化是在损失函数后面加上**参数平方和**，倾向于让参数变小，让模型参数整体更平滑，防止某些权重过大，提升泛化能力

10. “涌现能力”是大型模型中一个备受关注的现象，请问你如何理解这个概念？它通常在模型规模达到什么程度时出现？

11. **激活函数有了解吗，你知道哪些LLM常用的激活函数？为什么选用它？**

    ReLU、SiLU（做了一个平滑）、GELU（标准高斯分布的累积分布函数）、SwiGLU（用一条分支生成内容，用另一条分支生成门控，再通过逐元素相乘实现更灵活的特征选择）

    梯度更平滑，训练效果更好，表达能力更强

12. 混合专家模型（MoE）是如何在不显著增加推理成本的情况下，有效扩大模型参数规模的？请简述其工作原理。

    总参数稠密存储，单次前向稀疏激活

13. 在训练一个百或千亿参数级别的 LLM 时，你会面临哪些主要的工程和算法挑战？（例如：显存、通信、训练不稳定性等）

    1. 显存与存储压力：因为训练一个超大模型时，GPU 上需要放参数、梯度、优化器状态、主参数权重副本，单卡放不下

       1. 分布式并行（3D并行 工业标配）：
          * DP、TP、PP
          * DeepSpeed 提出的 ZeRO（Zero Redundancy Optimizer）/ FSDP（ZeRO-3）：**把参数、梯度、优化器状态做分片**
       
       2. 显存优化:
       
          * 混合精度训练（AMP）：混合精度训练用 fp16 做前反向、fp32 做参数更新，配合 Loss Scaling 防止下溢
          * 梯度检查点（Gradient Checkpointing）：只保存部分层激活，反向时重新计算 → **用计算换显存**
          * FlashAttention：利用 **SRAM 分块计算**，避免 HBM 读写注意力矩阵
       
       3. **通讯优化**：当模型大到单卡甚至单机都放不下时，就必须做分布式训练，通讯会成为瓶颈
       
          * 机器内部走 NVLink/NVSwitch——TP
          * 机器之间走 IB——PP
       
       4. 训练/算法：
       
          * 训练稳定性：大模型容易出现 loss spike、梯度爆炸、数值溢出、训练后期发散、小 bug 在长时间训练中被放大。可以使用梯度裁剪、Pre-LayerNorm（将 LayerNorm 移到 Attention/FFN 之前，训练更稳），学习率 Warmup
       
          * 学习率调度：Warmup + Cosine Decay
            $$
            \text{Cosine Decay：} lr_t = lr_{min} + \frac{1}{2}(lr_{max} - lr_{min})\left(1 + \cos\frac{\pi t}{T}\right)
            $$
       
          * 数据质量与配比：数据质量 >> 数据数量：需要严格的清洗 pipeline（去重、过滤低质量文本）；**Scaling Law**下的最有配比，计算量，模型大小和数据量应同步扩展
          * 长上下文训练：先训短序列，再用 **RoPE Scaling + 长文本继续训练** 扩展

    > 通讯
    >

    

14. 解释一下GPT（Decoder-Only）和BERT（Encoder-Only）在架构和训练目标上的根本区别。为什么在生成式任务上GPT更优？

    架构：单向注意力掩码；双向注意力掩码。

    训练目标：自回归语言建模（交叉熵）；MLM+NSP

    自回归的方式跟真实推理过程完全一致

15. 详细说明Transformer中的Self-Attention机制的计算公式（Attention(Q,K,V)=softmax(QKTdk)VAttention(*Q*,*K*,*V*)=softmax(*d**k**Q**K**T*)*V*）。为什么需要除以 dk*d**k*？

    * 除以 dk是为了**抵消点积随维度增长而增大的方差**，避免 softmax 进入梯度饱和区，确保训练稳定和模型收敛。

16. 请对比绝对位置编码（如原始Transformer）和相对位置编码（如RoPE）的优缺点

    优点：没有额外训练参数、只需要做一次加法；缺点：缺乏相对位置信息，外推能力差（没有先验知识）

    优点：天然支持相对位置信息，可以外推（结合插值PI NTK-aware YaRN）；缺点：与MLA不兼容

17. 什么是FlashAttention？它主要解决了 Transformer 的什么痛点？（可以从IO感知、Tiling技术、重计算等角度展开）

    * **FlashAttention** 是一种高效加速计算 Transformer 注意力机制的方法。它的核心思想是通过分块（tiling）和重计算（recomputation）技术，显著减少对高带宽内存（HBM）的访问次数，从而提升计算速度并降低显存访问

      - 前向 tilling：对于每个QKV block 加载到 SRAM，计算局部的注意力权重，维护当前最大值和累计归一化权重，这样扫描一遍所有块就能得到正确的 Softmax 结果。**每个块只从 HBM 读一次，最终结果只写一次 HBM**，大幅减少 IO。

      - 反向传播 recomputation：重新读 Q, K, V → 重新计算注意力矩阵 → 用于梯度计算。也是用计算换显存
    
    * 内存占用从 O(N^2) 降低到 O(N)，使得在相同显存下可以处理更长的序列（例如用单卡 A100 处理 64k 长度的序列）

18. 解释一下SwiGLU激活函数。相比于ReLU或GeLU，它在LLM中有什么优势？

19. 介绍一下常用的几种Norm（归一化）方法，如LayerNorm、RMSNorm。为什么LLM中更倾向于使用RMSNorm？

    BatchNorm：因为序列长度可变，且 batch 内样本的统计不稳定；同时训练与推理的统计量不一致，影响生成任务的稳定性

    LayerNorm：需要计算均值和方差

    RMSNorm：仅需计算平方和再开方，**节省了均值计算和中心化操作**，性能相当

20. 什么是数据清洗和去重？在大模型预训练中，为什么数据质量比数据量更重要？

    * 低质量数据的危害：**有毒、偏见的内容**直接注入模型；噪声数据**稀疏梯度**，无效参数更新；重复数据导致**过拟合**，模型多样性、泛化能力差；

    **数据清洗是指从原始语料中移除低质量、有害或无用内容的过程，是一个多阶段的pipeline**

    1. 语言过滤：用 fastText 等工具过滤掉**目标语言置信度**低的文档

    2. 文档级质量过滤：基于统计规则，包括：文本长度过短（无意义片段）、字符/词 比例异常（乱码）、数字/特殊字符占比过高（代码噪声、爬虫）、行重复率过高（模板、广告）、平均词长异常（非自然语言）、标点符号密度（低质量）

    3. 内容安全层面的过滤：

       * 关键词黑名单：色情、暴力、仇恨言论等关键词列表、URL 黑名单（已知有害网站）

       * 分类器过滤：训练二分类器识别有害内容；NSFW 分类器（not safe for word）

    4. 启发式规则规律：这是工业界大量使用的经验规则，包括**文档完整性**（完整句子、有无标题正文、段落数量）；重复 n-gram 检测

    5. 模型打分过滤：用一个小型高质量语言模型（如在 Wikipedia 上训练的 KenLM）对每篇文档打困惑度 PPL
       $$
       \text{PPL} = \exp\left(-\frac{1}{N}\sum_{i=1}^N \log P(w_i | w_{<i})\right)
       $$

       - PPL 太高：文本混乱，不像自然语言 → 过滤
       - PPL 太低 文本过于简单重复 → 过滤
       - 保留中间范围的文档

    **去重（deduplication）是指识别并移除语料库中重复或高度相似的文档，防止模型多次学习相同内容。**

    * 去重粒度：

      ```
      字符级    → 完全相同的文档 
      段落级    → 相同段落出现在不同文档 
      n-gram级  → 部分内容重复 
      语义级    → 语义相似但字面不同（最难）
      ```

    1. 精准去重：用哈希函数对文档生成指纹，完全相同则删除（差一个字就放过了）
    2. 工业界主流是 **MinHash + LSH**（模糊去重）：先把文档转为 n-gram 集合，用多个哈希函数生成 MinHash 签名，签名中相同位置的比例近似等于 Jaccard 相似度；再用 LSH 把签名分成多个 band，同一 band 完全相同的文档进入候选对，避免了 $O(n^2) $ 的暴力比较
    3. 语义去重（Embedding去重）：用模型生成文档 embedding，计算余弦相似度
    4. 跨数据集去重：还需要做**训练集和评测集之间的去重**，防止数据污染（data contamination）：

21. 什么是ZeRO（Zero Redundancy Optimizer）？ZeRO-1、ZeRO-2、ZeRO-3分别切分了哪些状态（优化器状态、梯度、参数）？

    * ZeRO**（**Zero Redundancy Optimizer）是微软 DeepSpeed 团队提出的一种**分布式训练优化技术**，旨在解决大模型训练中**显存不足**的问题。其核心思想是将**模型状态（优化器状态、梯度、参数）**在数据并行的各个 GPU 上进行切分（分片），从而消除数据并行中的冗余显存占用，使单卡可以承载更大的模型。
      1. ZeRO-1：切分优化器状态，每个GPU在自己的mini-batch上前向传播，计算Loss，然后进行反向传播计算梯度，通过all-reduce规约计算全局平均梯度再广播回每张卡，然后根据每张卡存储的优化器状态进行对应的参数更新，最后通过广播同步所有参数
      2. ZeRO-2：切分优化器状态和梯度。前向传播一致，就是在反向传播计算梯度的时候，也是每张GPU独立的计算所有的参数梯度但是不保存，然后进行 **Reduce-scatter**，计算全局的梯度平均值然后每张卡保留其负责的分片梯度。然后优化器更新对应参数，最后进行all-gather参数同步
      3. ZeRO-3：切分优化器状态、梯度、参数。本质就是在ZeRO-2基础上，增加了在每层前向和反向的参数All-gather

    * 体现了**用通信换显存**的核心思想 **trade-off**

22. 什么是SFT（监督微调）？为什么在预训练之后还需要SFT？

    * SFT（Supervised Fine-Tuning） 是指在大语言模型完成无监督预训练之后，使用人工标注或高质量筛选的“prompt-response”对（例如指令与期望回答），对模型进行有监督的训练，Loss 只计算在**回复部分**，让模型具备基本的指令跟随能力、适应特定任务或领域。

    * 预训练阶段的目标是**预测下一个token**，模型学到的是：给定上下文，预测下一个token的概率分布。模型从海量文本（网页、书籍、代码等）中学到了丰富的**语言知识、语法和事实**，但它学会的只是一种“续写”能力。

23. 量化是什么？

    量化是指将模型参数（权重）和/或激活值从**高精度浮点数**压缩为**低精度整数或低位浮点数**表示，从而减少模型体积和推理计算量。
    $$
    x_q = \text{round}\left(\frac{x}{s}\right) - z \\
    x_{dequant} = s \cdot (x_q + z)
    $$
    
    - $s $（scale）：缩放因子
    - $z $（zero point）：零点偏移
    - $x_q $：量化后的整数值
    
    通常都是**线性量化**：
    
    ​	对称（矩阵参数）：零点=0， 仅需缩放因子（通过最大值和量化bit数计算）
    
    ​	非对称（激活值）：引入零点（最小值计算），量化为对称区间
    
    * 训练后量化（PTQ，Post-Training Quantization）：用少量校准数据统计一下各层激活值的范围，然后确定缩放因子，把权重和激活值转换成低精度
    * 量化感知训练（QAT，Quantization-Aware Training）：在训练过程中模拟量化误差，让模型"适应"量化（插入“伪量化”节点，前向传播用量化低精度模拟，反向传播用反量化高精度浮点更新权重）
    
24. 混合精度（Mixed Precision）是指在同一个模型的**不同计算阶段**或不同组件中，混合使用多种精度，而不是所有地方统一用一种精度。让计算密集的部分（矩阵计算）用低精度跑得快，让精度敏感的部分用高精度保稳定

    * 纯 fp32 训练太慢、太占显存；纯 fp16 精度不够、容易溢出。混合精度就是**取两者之长**。

25. 混合精度训练中，为什么需要使用 Loss Scaling？梯度溢出的原因是什么？

    *  Loss Scaling（损失缩放） 是一种关键技术，用于防止梯度在反向传播过程中梯度过小而精度又不够导致的**下溢（**underflow**）**，从而保证训练稳定性和收敛性。
    * 原因：训练中梯度往往非常小，如果使用精度较小的fp16等，会四舍五入直接为0，导致参数不更新，训练停滞
    * **Loss Scaling** 的核心思路是：在反向传播之前，将损失值乘以一个较大的缩放因子 s（如1024），然后进行反向传播。这样，所有梯度也同步放大了 s倍，从而将其值提升到可表示的安全范围内。在参数更新之前再除回来

26. 显存优化方法（减少显存占用）：CPU Offloading、混合精度、3D并行（TP、PP+FSDP / ZeRO）、FlashAttention、Sparse Attention

27. LoRA与QLoRA

    rank r 越大表达能力越强，参数量越多

    优点：

    * 参数量极少，显存需求大幅降低
    * 训练速度快
    * 多个 LoRA 权重可以按需切换，不需要存多份完整模型
    * 推理时可合并，无额外延迟

    QLoRA：LoRA 解决了参数量的问题，但基座模型本身还是要加载进显存的。QLoRA的做法就是：把基座模型量化到 4-bit，再在上面挂 LoRA，用单张消费级 GPU 实现大模型微调。

    * NF4 normal float 4量化（非线性）：通过把正态分布曲线（神经网络权重近似服从正态分布）**等面积切分**来确定量化点位置，中间密两端稀，在 4-bit 的精度下实现信息损失最小化。
    * 双重量化double quantization：对缩放因子也进行量化，进一步节省显存

    优点：

    * 极大降低硬件门槛，消费级 GPU 可微调大模型
    * 精度损失相对较小（NF4 + 双重量化设计精良）

    缺点：

    * 训练速度比LoRA慢（反量化开销）
    * 推理时不能直接合并权重（基座是量化的，类型不匹配）

    **本质上是用速度换显存**

28. ALiBi（Attention with Linear Biases）位置编码是如何工作的？

    * 不将位置信息嵌入到词向量或者KV向量中，而是在计算注意力分数时，直接添加一个与 token 之间距离成正比的线性惩罚偏置项，用于惩罚远距离依赖减弱的情况
      $$
      \text{Attention score}_{ij} = \frac{q_i k_j^T}{\sqrt{d_k}} - m \cdot |i - j|
      $$

    * ALiBi 中不同头使用不同的斜率 m，以捕捉不同粒度的距离偏好。

      * 大斜率 head：距离惩罚强，只关注近邻 token，捕捉局部信息
      * 小斜率 head：距离惩罚弱，可以关注远距离 token，捕捉全局信息

      $$
      m_h = \frac{1}{2^{h \cdot 8/H}}, \quad h = 1, 2, ..., H
      $$

29. vLLM 的 PagedAttention

    内部碎片：实际序列长度往往小于最大长度，分配的内存中有一部分从未使用

    外部碎片：多个请求的 KV Cache 在显存中连续分配，当某些请求结束后释放空间，留下的空闲区域大小不一、分散各处，无法满足新请求的大块连续分配

    * PagedAttention 通过**分页+非连续存储**的方式：将 KV Cache 切分为**固定大小的block**（例如每块存储 16 个 token 的 KV），通过一个**Block Table**维护逻辑块到物理块的映射，每个序列的 KV Cache 由若干块组成，这些块在物理上可以**非连续存储**。这样就解决了碎片问题

    * 同时，pageattention还支持共享 KV Block ，对于相同前缀（如 System Prompt 或者GRPO重要性采样），多个请求可以通过 Block Table 共享同已有的KV cache Block，类似操作系统的 Copy-on-Write，只有需要写入时才复制，可以复用prompt的KV Block

30. 连续批处理（Continuous Batching）如何提升推理吞吐量？与传统静态批处理有何区别？

    * 传统静态批处理：一个batch中的请求必须等最长请求生成完毕后才能返回；某些请求提前完成了，剩余的GPU算力无法释放给新请求，必须等待整个batch结束

    * 连续批处理：不以 Batch 为调度单位，而是以单个生成 token 步为调度单位，每生成一个 token 都检查有无请求完成，动态调整 Batch 的组成。
    * 具体做法：同一个batch中混合了prefill和decode阶段的请求，**Chunked Prefill** 把大 Prefill 切成小块，与 Decode 交替执行，避免长 Prefill 阻塞 Decode 请求，降低首 token 延迟（TTFT）

    * 难点：同一个batch中混合了prefill和decode阶段的请求，这种混合会导致计算不均匀（这就是长 Prefill 阻塞 Decode 的问题）。Batch 内的多个请求是被打包成一个大矩阵，在同一次 GPU 前向传播中一起计算的

      * Decode 阶段好打包：每个请求每步只生成 1 个 token；所有请求步调一致，矩阵形状规整，天然适合打包

      * Prefill 阶段难打包：不同请求的 prompt 长度不同；

31. Depseek-V3和R1以及Qwen-2.5三个模型在架构上的区别

    Deepseek-V3是一个以**极致训练效率**为核心目标设计的 MoE 模型。架构创新：在注意力机制上提出了MLA，大幅压缩了KV Cache；使用了DeepseekMoE，对标准的MoE做了两个主要改进；还提出了MTP，在训练时不是预测下一个token，而是同时预测多个token；大量使用了FP8混合精度进行训练

    Deepseek-R1架构基础和V3完全一样，但在**训练方法**有所不同，核心就是用RL激发推理能力

    Qwen-2.5是稠密模型：注意力机制上使用了GQA，使用 **YaRN** 实现上下文长度外推。

32. 以7B模型举例，计算**训练**需要多少显存，有哪些部分组成

    16x法则：

    * 模型权重（FP16精度，14G）
    * 梯度（14G）
    * 优化器状态（一阶二阶动量+主权重，FP32，28+28+28GB）

    激活值：

    * QKV矩阵（主要）+ **Attention score**、FFN层中间结果、残差分支等等

      激活值显存 = c（10～20） * batch_size * sequence_length * hidden_size * Layer * 2 byte（FP / BF16）

      c 在使用 gradient checkpoint 之后会显著下降

    注意：一般问需要多少显存需要分训练和推理

    * 训练显存：参数 + 梯度 + 优化器状态 + 激活值
    * 推理显存：参数+ KV Cache

      > 推理显存优化：模型量化；pagedAttention、GQA MQA MLA

33. Gradient Checkpointing（梯度检查点）如何提升计算效率（节约激活值显存）

    **计算换显存**的策略：只保存部分层的激活值（检查点），其余层的激活值在反向传播时需要用到时再根据最近的checkpoint重新计算。

34. Adam与SGD的区别，优缺点

    | 维度         | SGD（Momentum）                | Adam                   |
    | :----------- | :----------------------------- | :--------------------- |
    | **学习率**   | 全局固定，需手动调整           | 每个参数自适应变化     |
    | **梯度利用** | 仅使用一阶动量（累积梯度方向） | 同时使用一阶和二阶动量 |

36. SFT如何构建数据集，数据量多少

    1. 人工标注
    2. 教师模型蒸馏（多教师融合）
    3. 拒绝采样 
    4. 真实用户交互数据

37. 多模态基本训练流程

    

38. 多模态训练的时候为什么要冻结图像部分的参数？
    ​    

# RLHF

+++

1. 和传统SFT相比，RLHF旨在解决语言模型中的哪些核心问题？为什么说SFT本身不足以实现我们期望的“对齐”目标？

   * 和传统 SFT 相比，RLHF 主要是为了解决“人类偏好无法通过单一标准答案充分表达”的问题。
   * 也就是说，SFT 能让模型学会“像样地回答”，但很难进一步保证模型回答更符合人类偏好、更有帮助、更安全、更自然（Helpful、Honest、Harmless）。而 RLHF 的目标，就是在 SFT 的基础上，继续把模型往“人类真正喜欢的回答方式”上推近一步。

2. 请详细阐述经典RLHF流程的三个核心阶段。在每个阶段，输入是什么，输出是什么，以及该阶段的关键目标是什么？

   阶段1：SFT

   - **输入**：prompt + 人类示范回答
   - **输出**：SFT 模型
   - **目标**：让模型具备指令跟随和基本对话能力

   阶段2：Reward Model

   - **输入**：prompt + 多个候选回答 + 人类偏好排序
   - **输出**：奖励模型 RM（BT）
   - **目标**：学习一个可以近似人类偏好的奖励函数

   阶段3：RL（PPO）

   - **输入**：prompt + SFT 初始化策略 + RM + reference model
   - **输出**：RLHF 后的对齐模型
   - **目标**：在不偏离参考模型能力太多的前提下，最大化人类偏好奖励

   

3. 在RM训练阶段，我们通常收集的是成对比较数据，而不是让人类标注者直接给回复打一个绝对分数。你认为这样做的主要优势和潜在的劣势分别是什么？

   成对比较的核心优势：更符合人类自然偏好表达方式，标注更稳定（相对判断的标注一致性高）、排序目标更清晰；

   核心劣势：只能提供相对顺序信息，缺少绝对质量刻度，而且偏好数据本身可能有噪声、偏差和不传递性。

4. 奖励模型的设计至关重要。它的模型架构通常如何选择？它与我们最终要优化的LLM是什么关系？在训练奖励模型时，常用的损失函数是什么？请解释其背后的数学原理（例如，可以结合Bradley-Terry模型来解释）。

5. 在RLHF的第三阶段，PPO是最主流的强化学习算法。为什么选择PPO，而不是其他更简单的策略梯度算法（如REINFORCE）或者Q-learning系算法？PPO中的KL散度惩罚项起到了什么关键作用？

   PPO 属于 off-policy 策略优化方法，更新更稳定，还能通过 clipping 和 KL 约束防止策略一下子跑偏

6. 如果在PPO训练过程中，KL散度惩罚项的系数 β 设置得过大或过小，分别会导致什么样的问题？你将如何通过实验和观察来调整这个超参数？

8. RLHF流程复杂且不稳定。近年来出现了一些替代方案，例如DPO。请解释DPO的核心思想，并比较它与传统RLHF（基于PPO）的主要区别和优势。

   通过一个配分函数Z，得到最优策略跟奖励函数之间的关系，然后通过BT偏好模型得到了DPO损失函数，

   DPO不使用奖励模型，拿偏好数据直接做带 reference 约束的对数概率的对比学习。

   对于一条数据 $(x, y_w, y_l)$，DPO 会比较：

   - 当前策略对 winner 和 loser 的对数概率差
   - 参考模型对 winner 和 loser 的对数概率差

   如果当前策略相对于参考模型，更倾向于 winner 而不是 loser，那么 loss 就会更小；反之就会被更新。

   * 优点：移除奖励模型训练和强化学习部分，实现端到端优化；直接使用分类损失，训练过程易于控制；内存占用更小
   * 缺点：仍然依赖高质量的偏好数据

9. 想象一下，你训练完成的RLHF模型在离线评估中表现优异，奖励模型分数很高，但上线后用户反馈其回答变得越来越“模式化”、奉承、且缺乏信息量。你认为可能的原因是什么？你会从哪些方面着手分析和解决这个问题？

   并不是训练没收敛，而是训练目标与真实产品目标没有完全一致。出现reward hacking

10. 你知道Deepseek的GRPO吗，**它和PPO的主要区别是什么**？优劣是什么？

   GRPO 是 DeepSeek 提出的 PPO 变体，核心思想是取消 PPO 里额外训练的 value/critic 模型，对同一个 prompt 一次采样多条回答，用这组回答的相对奖励经过组均值和组标准差归一化后，直接作为优势信号来更新 policy。相比 PPO，GRPO 最大的区别是不用单独学 value function，因此显存和训练开销更低；同时它更适合数学、代码这类有可验证奖励的推理任务。它的优势是简单、高效、资源占用低，

   

11. 如何解决信用分配问题？token级别和seq级别的奖励有何不同？

    一段回答最终只拿到一个结果奖励时，怎么把这个奖励合理分配回前面的生成过程

    

12. 除了人类反馈，我们还可以利用AI自身的反馈来做对齐，即RLAIF。请谈谈你对RLAIF的理解，它的潜力和风险分别是什么？

13. trust-range和PPO的关系

    Trust Region 是"每步更新不要走太远"的核心思想，TRPO 用显式 KL 约束精确实现但计算代价极大；PPO 用 Clip 操作把概率比限制在 $[1-\epsilon, 1+\epsilon] $ 内，以一阶优化近似实现信任域效果，大幅降低了实现复杂度；在 RLHF 中两者叠加使用——Clip 约束单步更新幅度，KL 惩罚项约束整体不偏离参考模型

14. 为什么会有**importance sampling**，采样的策略模型和要训练的策略模型有偏差，重要性采样加以修正

    重要性采样的本质是**用概率比 $r_t = \pi_\theta / \pi_{old} $ 修正采样分布的偏差**，让旧策略采集的数据可以用来估计新策略的期望，从而实现数据复用；但新旧策略差距过大时概率比会极端导致训练不稳定，PPO 的 Clip 操作限制了这个修正幅度，在数据复用和训练稳定之间取得平衡。

15. PPO优势计算

    优势：比我预期的好多少 = 实际得到的收益 - 预期能得到的收益

    算 TD 残差 $\delta_t $： 就是"单步的优势估计"
    $$
    \delta_t = r_t + \gamma V(s_{t+1}) - V(s_t)
    $$
    用 GAE 把多步 $\delta $ 加权累加，从序列末尾往前算。得到优势

    

16. GRPO的损失计算，在序列级别的损失上，损失如何给到每一个token上?序列级别平均或批次级别平均

    GRPO 把序列级奖励标准化后得到序列级优势，将其广播到该序列每个 token 上，token 级概率比独立计算后与序列级优势相乘得到每个 token 的 loss；原始论文和 DeepSeek-R1 使用**序列级平均**（先按序列长度归一化再对 G 条序列平均），让每条回答平等贡献梯度，避免长序列主导训练。





# 模型评估与Agent评估

+++

1. 为什么传统的 NLP 评估指标（如 BLEU, ROUGE）对于评估现代 LLM 的生成质量来说，存在很大的局限性？
2. 请介绍几个目前行业内广泛使用的 LLM 综合性基准测试，并说明它们各自的侧重点。（例如：MMLU, Big-Bench, HumanEval）
3. 什么是“LLM-as-a-Judge”？使用 LLM 来评估另一个 LLM 的输出，有哪些优点和潜在的偏见？
4. 如何设计一个评估方案来衡量 LLM 的特定能力，比如“事实性/幻觉水平”、“推理能力”或“安全性”？
5. 评估一个 Agent 为什么比评估一个基础 LLM 更加困难和复杂？评估的维度有哪些不同？
6. 你了解哪些专门用于评估 Agent 能力的基准测试？这些基准通常如何构建测试环境和任务？
7. 在评估一个 Agent 的任务完成情况时，除了最终结果的正确性，还有哪些过程指标是值得关注的？（例如：效率、成本、鲁棒性）
8. 什么是红队测试？它在发现 LLM 和 Agent 的安全漏洞与偏见方面扮演着什么角色？
9. 在进行人工评估时，如何设计合理的评估准则和流程，以保证评估结果的客观性和一致性？
10. 如何持续监控和评估一个已经部署上线的 LLM 应用或 Agent 服务的表现，以应对可能出现的性能衰退或行为漂移？

