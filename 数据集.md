# Benchmark

+++

**MMLU（Massive Multitask Language Understanding）**
57 个科目的多选题，以准确率作为指标，衡量模型的知识广度。

如果你还看到类似名字：**CMMLU**（中文 MMLU）、**MMLU-Pro**（更难/更注重推理）等，它们基本都是在 MMLU 思路上做的扩展或改进。

+++

**HumanEval / MBPP**
代码生成任务，通过“pass@k”指标（生成 k 个候选，只要有一个通过单元测试即为正确）衡量模型的编程能力。

+++

**GSM8K / MATH**
数学推理任务，以准确率（答案匹配）或“严格准确率”衡量。

+++

**LongBench**

目前最广泛使用的长文本基准之一，包含 **6 大类、21 个子任务**，覆盖单文档问答、多文档问答、摘要、代码生成、长对话、数学推理等。





# 指标

+++

- 使用 **准确率、F1、EM** 等用于分类/选择任务；
- 使用 **ROUGE、BERTScore** 等用于生成任务；
