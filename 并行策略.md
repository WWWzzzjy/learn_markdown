# 并行策略

+++

### 1. 数据并行（Data Parallelism, DP）

每张卡复制完整模型，数据切分：

```
卡0: 完整模型 + batch 0 → 梯度0 ─┐
卡1: 完整模型 + batch 1 → 梯度1 ─┤→ AllReduce → 同步梯度 → 更新
卡2: 完整模型 + batch 2 → 梯度2 ─┘
```

- ✅ 实现简单，吞吐高
- ❌ 每卡仍需存完整模型，显存没有减少



### 2. 张量并行（Tensor Parallelism, TP）

将单个矩阵运算**按列或行切分**到多卡：

​	以 MLP 的线性层 $Y = XW $ 为例：

​	**按列切分（Column Parallel）：**
$$
W = [W_1 | W_2], \quad Y = [XW_1 | XW_2]
$$
​	**按行切分（Row Parallel）：**
$$
W = \begin{bmatrix} W_1 \\ W_2 \end{bmatrix}, \quad X = [X_1, X_2], \quad Y = X_1W_1 + X_2W_2
$$

```
Attention/MLP 内部切分：
卡0: W[:, :d/2]   卡1: W[:, d/2:]
       ↓                  ↓
     部分结果           部分结果
       └──── AllReduce ────┘
```

- ✅ 显存按卡数线性减少
- ❌ 通信频繁，要求卡间高带宽（NVLink）
- ❌ 实现复杂，需要重写算子

Megatron-LM 是 TP 的经典实现。



### 3. 流水线并行（Pipeline Parallelism, PP）

将模型**按层切分**，不同卡负责不同层：

```
卡0: Layer 1-8   → 激活值
卡1: Layer 9-16  → 激活值
卡2: Layer 17-24 → 激活值
卡3: Layer 25-32 → 输出
```

**Naive Pipeline 的问题 —— 气泡（Bubble）：**

```
时间轴：
卡0: [前向] [等待] [等待] [反向]
卡1:        [前向] [等待] [反向]
卡2:               [前向] [反向]
               ↑ bubble，GPU 空闲浪费
```

**解决方案 —— Micro-batch 流水（GPipe / 1F1B）：**

```
1F1B 调度：
卡0: F0 F1 F2 F3 B3 B2 B1 B0
卡1:    F0 F1 F2 F3 B3 B2 B1 B0
气泡比例 ≈ (p-1)/(m+p-1)，m 为 micro-batch 数
```

- ✅ 显存按流水段数减少
- ❌ 气泡开销，通信延迟