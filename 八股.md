# LLM

+++

1. 请详细解释一下 Transformer 模型中的自注意力机制是如何工作的？它**为什么比 RNN 更适合处理长序列？**

   QKV向量，计算当前token的q对之前所有token的k做点积，计算注意力权重，然后对v做加权求和

   RNN处理序列是逐步递进的，每一步的隐藏态依赖于前一个隐藏态。反向传播沿着时间步，序列越长梯度容易消失，无法关联长距离依赖；无法并行计算，训练慢

2. 什么是位置编码？在 Transformer 中，为什么它是必需的？**请列举至少两种实现方式。**

3. 请你详细介绍RoPE，**对比绝对位置编码它的优劣势分别是什么？**

   绝对位置编码，直接与token embedding相加，得到的是绝对位置信息而不是token之间相对位置信息；长度外推能力差。

4. 你知道MHA，GQA，MQA，MLA的区别吗？详细解释一下。

   多头可以关注到更多的语义信息，

5. 请比较一下几种常见的 LLM 架构，例如 Encoder-Only, Decoder-Only, 和 Encoder-Decoder，并说明它们各自最擅长的任务类型。

   双向 Self-Attention（每个 token 可以看到序列中所有其他 token）随机遮盖部分 token，让模型预测被遮盖的词

6. 什么是Scaling Laws？它揭示了模型性能、计算量和数据量之间的什么关系？这对LLM的研发有什么指导意义？

   描述了语言模型的**损失（Loss）与模型参数量 $N $、训练数据量 $D $、计算量 $C $ 之间的幂律关系（Power Law）** ：

7. 在LLM的推理阶段，有哪些常见的解码策略？请解释 Greedy Search, Beam Search, Top-K Sampling 和 Nucleus Sampling (Top-P) 的原理和优缺点。

   

8. **什么是词元化？请比较一下 BPE 和 WordPiece 这两种主流的子词切分算法。**

   词元化是将原始文本切分成模型可以处理的**最小单元（Token）**的过程，然后将每个 token 映射到一个整数 ID，作为模型的输入。

   切分粒度：词、字符、子词（高频词保留，低频词拆成更小的子词单元）

   BPE：初始化词表为所有单字符，统计语料中所有**相邻 token 对**的频率，将频率最高的 token 对合并为一个新 token，加入词表

   wordPiece：初始化词表为所有单字符，每次选择合并后**能最大化语言模型似然**的 token 对：

   Unigramn：自顶向下，基于语言模型概率逐步删减子词

9. **你觉得NLP和LLM最大的区别是什么？两者有何共同和不同之处？**

   

10. **L1和L2正则化分别是什么，什么场景适合使用呢？**

    L1 和 L2 正则化，本质上都是在原始损失函数后面加一个“参数惩罚项”，目的是限制模型复杂度，防止过拟合。

    L1 正则化是在损失函数后面加上**参数绝对值之和**，抑制过拟合，让参数变得稀疏；

    L2 正则化是在损失函数后面加上**参数平方和**，倾向于让参数变小，让模型参数整体更平滑，防止某些权重过大，提升泛化能力

11. “涌现能力”是大型模型中一个备受关注的现象，请问你如何理解这个概念？它通常在模型规模达到什么程度时出现？

12. 激活函数有了解吗，你知道哪些LLM常用的激活函数？为什么选用它？

    ReLU、SiLU（做了一个平滑）、GELU（标准高斯分布的累积分布函数）、SwiGLU（用一条分支生成内容，用另一条分支生成门控，再通过逐元素相乘实现更灵活的特征选择）

    梯度更平滑，训练效果更好，表达能力更强

13. 混合专家模型（MoE）是如何在不显著增加推理成本的情况下，有效扩大模型参数规模的？请简述其工作原理。

    总参数稠密存储，单次前向稀疏激活

14. 在训练一个百或千亿参数级别的 LLM 时，你会面临哪些主要的工程和算法挑战？（例如：显存、通信、训练不稳定性等）

    显存与存储压力：因为训练一个超大模型时，GPU 上放参数、梯度、优化器状态、激活值

    解决方法:

    **混合精度训练**，比如 BF16 / FP16

    **ZeRO / FSDP**，把参数、梯度、优化器状态做分片

    **CPU / NVMe Offload**，把部分状态卸载到更慢的存储层

    **FlashAttention**，减少 attention 的显存访问和中间张量

    

    分布式并行与通信开销：当模型大到单卡甚至单机都放不下时，就必须做分布式训练，通讯会成为瓶颈

    并行策略：DP、TP、PP、SP、Expert Parallel（MoE）

    数据并行要做梯度 All-Reduce

    张量并行层间要频繁做 All-Gather / Reduce-Scatter

    Pipeline Parallel 会有 stage 间传输和 bubble 问题

    MoE 还会带来 token 到 expert 的 All-to-All 通信

    

    训练稳定性问题：模型一旦进入百亿、千亿级别，训练不稳定会被明显放大。

    梯度爆炸 / 梯度异常、loss spike、数值溢出、训练后期发散、小 bug 在长时间训练中被放大

    

    数据质量与训练效率挑战：因为如果数据质量差、重复多、噪声重，就会出现训练效率低、泛化性差

    

    算法上的相关问题：Scaling law 下的最优配比：参数量、数据量、训练 token 数之间要平衡，不是参数越大越好

    

15. 解释一下GPT（Decoder-Only）和BERT（Encoder-Only）在架构和训练目标上的根本区别。为什么在生成式任务上GPT更优？

    架构：单向注意力掩码；双向注意力掩码。自回归语言建模（交叉熵）；MLM+NSP

    自回归的方式跟真实推理过程完全一致

    

16. 详细说明Transformer中的Self-Attention机制的计算公式（Attention(Q,K,V)=softmax(QKTdk)VAttention(*Q*,*K*,*V*)=softmax(*d**k**Q**K**T*)*V*）。为什么需要除以 dk*d**k*？

    除以 dk*d**k* 是为了**抵消点积随维度增长而增大的方差**，避免 softmax 进入梯度饱和区，确保训练稳定和模型收敛。

17. 请对比绝对位置编码（如原始Transformer）和相对位置编码（如RoPE）的优缺点

    优点：没有额外训练参数、只需要做一次加法；缺点：缺乏相对位置信息，外推能力差（没有先验知识）

    优点：天然支持相对位置信息，可以外推（结合插值PI NTK-aware YaRN）；缺点：与MLA不兼容

<<<<<<< HEAD
    

=======
>>>>>>> 874398bac1362046909f3c4c7f01b8f84fc448df
18. 什么是FlashAttention？它主要解决了Transformer的什么痛点？（可以从IO感知、Tiling技术、重计算等角度展开）

    **FlashAttention** 是一种高效实现 Transformer 注意力机制的方法。它的核心思想是通过分块（tiling）和重计算（recomputation）技术，显著减少对高带宽内存（HBM）的访问次数，从而提升计算速度并降低显存占用

    - 对于每个 Qi*Q**i*，依次加载 Kj,Vj*K**j*,*V**j* 到 SRAM，计算局部的注意力权重和输出，并将结果与之前累积的合并。

    内存占用从 O(N2) 降低到 O(N)，使得在相同显存下可以处理更长的序列（例如用单卡 A100 处理 64k 长度的序列）

    FlashAttention 的目标是**尽可能将计算在 SRAM 中完成，避免中间结果落盘到 HBM**。通过将注意力计算分解成多个块（tiles），每个块可以完全在 SRAM 中处理，从而大幅减少 HBM 的读写次数。

<<<<<<< HEAD
    

19. 解释一下SwiGLU激活函数。相比于ReLU或GeLU，它在LLM中有什么优势？

=======
19. 解释一下SwiGLU激活函数。相比于ReLU或GeLU，它在LLM中有什么优势？

>>>>>>> 874398bac1362046909f3c4c7f01b8f84fc448df
20. 介绍一下常用的几种Norm（归一化）方法，如LayerNorm、RMSNorm。为什么LLM中更倾向于使用RMSNorm？

    BatchNorm：因为序列长度可变，且 batch 内样本的统计不稳定；同时训练与推理的统计量不一致，影响生成任务的稳定性

    LayerNorm：需要计算均值和方差

    RMSNorm：仅需计算平方和再开方，**节省了均值计算和中心化操作**，性能相当

21. 什么是数据清洗和去重？在大模型预训练中，为什么数据质量比数据量更重要？

    数据清洗是指从**原始语料**中移除低质量、有害或无用内容的过程。

    * 语言过滤：保留目标语言，剔除其他语言或乱码
    * 格式规范过滤：统一编码（UTF-8）、去除 HTML/XML 标签、修复 Unicode 异常字符
    * 内容过滤：去除敏感、低质量（机器生产的无意义、重复、广告）、个人身份信息
    * 质量评分：使用启发式规则或者小模型进行文本质量打分

    去重（deduplication）是指识别并移除语料库中重复或高度相似的文档，防止模型多次学习相同内容。

    * 精准去重：使用哈希或者直接比对，移除完全相同的文档
    * 近似去重：嵌入相似度计算，移除内容高度重叠的文档
    * 子串去重：防止数据集中包含长文本的重复片段

    影响：低质量数据损害模型能力、性能瓶颈（过拟合重复内容、破坏多样性）、质量驱动已经被验证

22. 什么是ZeRO（Zero Redundancy Optimizer）？ZeRO-1、ZeRO-2、ZeRO-3分别切分了哪些状态（优化器状态、梯度、参数）？

    ZeRO**（**Zero Redundancy Optimizer）是微软 DeepSpeed 团队提出的一种**分布式训练优化技术**，旨在解决大模型训练中**显存不足**的问题。其核心思想是将**模型状态（优化器状态、梯度、参数）**在数据并行的各个 GPU 上进行切分（分片），从而消除数据并行中的冗余显存占用，使单卡可以承载更大的模型。

    ZeRO-1：切分优化器状态，每个GPU在自己的mini-batch上前向传播，计算Loss，然后进行反向传播计算梯度，通过all-reduce规约计算平均梯度再广播回每张卡，然后根据每张卡存储的优化器状态进行对应的参数更新，最后通过广播同步所有参数

    ZeRO-2：切分优化器状态和梯度。前向传播一致，就是在反向传播计算梯度的时候，也是每张GPU独立的计算所有的参数梯度但是不保存，然后进行Reduce-scatter，计算全局的梯度平均值然后每张卡保留其负责的分片梯度。然后优化器更新对应参数，最后进行all-gather参数同步

    ZeRO-3：切分优化器状态、梯度、参数。本质就是在ZeRO-2基础上，增加了在每层前向和反向的参数All-gather

    体现了**用通信换显存**的核心思想

23. 在大模型预训练中，如何避免“灾难性遗忘”？虽然这更多出现在增量预训练中，但根源在于预训练的知识分布。

    灾难性遗忘的根源在于预训练阶段形成的数据分布（差异太大）与增量训练阶段的数据分布不一致。

    * 数据：混合数据采样

    * 算法：进行知识蒸馏、正则化

      

24. 什么是SFT（监督微调）？为什么在预训练之后还需要SFT？

    SFT（Supervised Fine-Tuning） 是指在大语言模型完成无监督预训练之后，使用人工标注或高质量筛选的“prompt-response”对（例如指令与期望回答），对模型进行有监督的训练，让模型具备基本的指令跟随能力、适应特定任务或领域。

    预训练阶段的目标是**预测下一个词**。模型从海量文本（网页、书籍、代码等）中学到了丰富的**语言知识、语法和事实**，但它学会的只是一种“续写”能力。

25. 量化是什么？

    量化是指将模型参数（权重）和/或激活值从**高精度浮点数**压缩为**低精度整数或低位浮点数**表示，从而减少模型体积和推理计算量。

    通常都是**线性量化**：

    ​	对称（矩阵参数）：零点=0， 仅需缩放因子（通过最大值和量化bit数计算）

    ​	非对称（激活值）：引入零点（最小值计算），量化为对称区间

    * 训练后量化（PTQ，Post-Training Quantization）：用少量校准数据统计一下各层激活值的范围，然后确定缩放因子，把权重和激活值转换成低精度
    * 量化感知训练（QAT，Quantization-Aware Training）：在训练过程中模拟量化误差，让模型"适应"量化（插入“伪量化”节点，前向传播用量化低精度模拟，反向传播用反量化高精度浮点更新权重）

26. 混合精度（Mixed Precision）是指在同一个模型的**不同计算阶段**或不同组件中，混合使用多种精度，而不是所有地方统一用一种精度。

    让计算密集的部分用低精度跑得快，让精度敏感的部分用高精度保稳定

27. 混合精度训练中，为什么需要使用 Loss Scaling？梯度溢出的原因是什么？

    Loss Scaling（损失缩放） 是一种关键技术，用于防止梯度在反向传播过程中因数值过小而**下溢（**underflow**）**，从而保证训练稳定性和收敛性。

    原因：反向传播梯度逐层相乘，极易变得很小；学习率小；损失值小

    **Loss Scaling** 的核心思路是：在反向传播之前，将损失值乘以一个较大的缩放因子 s（如1024），然后进行反向传播。这样，所有梯度也同步放大了 s倍，从而将其值提升到可表示的安全范围内。

28. 显存优化方法（减少显存占用）：CPU Offloading、混合精度、ZeRO、FSDP、FlashAttention、Sparse Attention

29. LoRA与QLoRA

    rank r 越大表达能力越强，参数量越多

    优点：

    * 参数量极少，显存需求大幅降低
    * 训练速度快
    * 多个 LoRA 权重可以按需切换，不需要存多份完整模型
    * 推理时可合并，无额外延迟

    QLoRA：LoRA 解决了参数量的问题，但基座模型本身还是要加载进显存的。QLoRA的做法就是：把基座模型量化到 4-bit，再在上面挂 LoRA，用单张消费级 GPU 实现大模型微调。

    * NF4量化（非线性）：通过把正态分布曲线等面积切分来确定量化点位置，中间密两端稀，在 4-bit 的极限精度下实现信息损失最小化。
    * 双重量化double quantization：对缩放因子也进行量化，进一步节省显存

    优点：

    * 极大降低硬件门槛，消费级 GPU 可微调大模型
    * 精度损失相对较小（NF4 + 双重量化设计精良）

    缺点：

    * 训练速度比LoRA慢（反量化开销）
    * 推理时不能直接合并权重（基座是量化的，类型不匹配）

    **本质上是用速度换显存**

30. ALiBi（Attention with Linear Biases）位置编码是如何工作的？

    不将位置信息嵌入到词向量中，而是在计算注意力分数时，直接添加一个与 token 之间距离成正比的线性偏置项

    ALiBi 中不同头使用不同的斜率 m，以捕捉不同粒度的距离偏好。

31. vLLM 的 PagedAttention

    内部碎片：实际序列长度往往小于最大长度，分配的内存中有一部分从未使用

    外部碎片：多个请求的 KV Cache 在显存中连续分配，当某些请求结束后释放空间，留下的空闲区域大小不一、分散各处，无法满足新请求的大块连续分配

    PagedAttention 通过**分页+非连续存储**的方式：将 KV Cache 组织为**固定大小的逻辑块**（例如每块存储 16 个 token 的 KV），每个序列的 KV Cache 由若干块组成，这些块在物理上可以**非连续存储**，通过一个**块表**维护逻辑块到物理块的映射。

32. 连续批处理（Continuous Batching）如何提升推理吞吐量？与传统静态批处理有何区别？

    传统静态批处理：一个batch中的短请求必须等最长请求生成完毕后才能返回；某些请求完成了，剩余的GPU算力无法释放给新请求，必须等待整个batch结束

    连续批处理：不以 Batch 为调度单位，而是以单个 iteration（生成token步）为调度单位，每一步都动态调整 Batch 的组成。**Chunked Prefill** 把大 Prefill 切成小块，与 Decode 交替执行，避免长 Prefill 阻塞 Decode 请求，降低首 token 延迟（TTFT）

    难点：同一个batch中混合了prefill和decode阶段的请求，这种混合会导致计算不均匀（这就是长 Prefill 阻塞 Decode 的问题）。Batch 内的多个请求是被打包成一个大矩阵，在同一次 GPU 前向传播中一起计算的

    * Decode 阶段好打包：每个请求每步只生成 1 个 token；所有请求步调一致，矩阵形状规整，天然适合打包
    * Prefill 阶段难打包：不同请求的 prompt 长度不同；

33. Depseek-V3和R1以及Qwen-2.5三个模型在架构上的区别

    Deepseek-V3是一个以**极致训练效率**为核心目标设计的 MoE 模型。架构创新：在注意力机制上提出了MLA，大幅压缩了KV Cache；使用了DeepseekMoE，对标准的MoE做了两个主要改进；还提出了MTP，在训练时不是预测下一个token，而是同时预测多个token；大量使用了FP8混合精度进行训练

    Deepseek-R1架构基础和V3完全一样，但在**训练方法**有所不同，核心就是用RL激发推理能力

    Qwen-2.5是稠密模型：注意力机制上使用了GQA，使用YaRN实现上下文长度外推。

34. 以7B模型举例，计算训练需要多少显存，有哪些部分组成

    16x法则：

    * 模型权重（FP16精度，14G）
    * 梯度（14G）
    * 优化器状态（一阶二阶动量+主权重，FP32，28+28+28GB）

    激活值：

    * QKV矩阵、**Attention score**、attention输、FFN、Norm、transformer layer

35. Gradient Checkpointing（梯度检查点）如何提升计算效率（节约激活值显存）

    **计算换显存**的策略：只保存部分层的激活值（检查点），其余层的激活值在反向传播时需要用到时再根据最近的checkpoint重新计算。

36. Adam与SGD的区别，优缺点

<<<<<<< HEAD
    |              | SGD                            | Adam                   |
    | :----------- | :----------------------------- | ---------------------- |
    | **学习率**   | 全局固定，需手动调整           | 每个参数自适应变化     |
    | **梯度利用** | 仅使用一阶动量（累积梯度方向） | 同时使用一阶和二阶动量 |

37. SFT如何构建数据集，数据量多少

    Alpaca格式因其简洁性而成为事实上的标准，其核心是一个JSON字典，包含三个字段：

    - `instruction`: 任务指令。
    - `input`: 选填，为指令提供额外上下文。
    - `output`: 期望模型生成的正确答案。

38. GRPO和PPO
=======
37. SFT如何构建数据集，数据量多少

38. GRPO和PPO

    
>>>>>>> 874398bac1362046909f3c4c7f01b8f84fc448df

    


# 训练

+++

1. **预训练损失函数**

2. **SFT 损失函数**

3. **PPO 损失函数**

   

4. **DPO 损失函数**

5. **GRPO 损失函数**
   $$
   \mathcal{L}_{\text{GRPO}} = -\frac{1}{G} \sum_{i=1}^{G} \frac{1}{|y_i|} \sum_{t=1}^{|y_i|} \left[ \min\left(\rho_{i,t} \hat{A}_i,\ \text{clip}(\rho_{i,t}, 1-\varepsilon, 1+\varepsilon) \cdot \hat{A}_i \right) - \beta \cdot \underbrace{\log \frac{\pi_\theta(a_{i,t}|s_{i,t})}{\pi_{\text{ref}}(a_{i,t}|s_{i,t})}}_{\text{KL penalty per token}} \right]
   $$
   流程：

   1. 组内采样：对同一个 prompt，用旧策略一次性采样G条响应
      $$
      \{y_1, y_2, \dots, y_G\} \sim \pi_{\theta_{old}}(\cdot | x)
      $$

   2. 计算每条响应的 Reward（序列级的 Reward）

   3. 组内对G条响应得分进行标准归一化得到奖励A，并广播到每一个 token
      $$
      \hat{A}_i = \frac{r_i - \text{mean}(\{r_1,\dots,r_G\})}{\text{std}(\{r_1,\dots,r_G\})}
      $$

   4. 计算Loss，采用 Clip 限制策略更新到幅度，并加入逐个 token 到KL惩罚

6. **GSPO 损失函数**

   https://zhuanlan.zhihu.com/p/1932791271363154917

   

# RLHF

+++

1. 和传统SFT相比，RLHF旨在解决语言模型中的哪些核心问题？为什么说SFT本身不足以实现我们期望的“对齐”目标？

   和传统 SFT 相比，RLHF 主要是为了解决“人类偏好无法通过单一标准答案充分表达”的问题。
    也就是说，SFT 能让模型学会“像样地回答”，但很难进一步保证模型回答 更符合人类偏好、更有帮助、更安全、更自然（3H）。而 RLHF 的目标，就是在 SFT 的基础上，继续把模型往“人类真正喜欢的回答方式”上推近一步。

2. 请详细阐述经典RLHF流程的三个核心阶段。在每个阶段，输入是什么，输出是什么，以及该阶段的关键目标是什么？

   阶段1：SFT

   - **输入**：prompt + 人类示范回答
   - **输出**：SFT 模型
   - **目标**：让模型具备指令跟随和基本对话能力

   阶段2：Reward Model

   - **输入**：prompt + 多个候选回答 + 人类偏好排序
   - **输出**：奖励模型 RM（BT）
   - **目标**：学习一个可以近似人类偏好的奖励函数

   阶段3：RL（PPO）

   - **输入**：prompt + SFT 初始化策略 + RM + reference model
   - **输出**：RLHF 后的对齐模型
   - **目标**：在不偏离参考模型能力太多的前提下，最大化人类偏好奖励

   

3. 在RM训练阶段，我们通常收集的是成对比较数据，而不是让人类标注者直接给回复打一个绝对分数。你认为这样做的主要优势和潜在的劣势分别是什么？

   成对比较的核心优势：更符合人类自然偏好表达方式，标注更稳定（相对判断的标注一致性高）、排序目标更清晰；

   核心劣势：只能提供相对顺序信息，缺少绝对质量刻度，而且偏好数据本身可能有噪声、偏差和不传递性。

4. 奖励模型的设计至关重要。它的模型架构通常如何选择？它与我们最终要优化的LLM是什么关系？在训练奖励模型时，常用的损失函数是什么？请解释其背后的数学原理（例如，可以结合Bradley-Terry模型来解释）。

5. 在RLHF的第三阶段，PPO是最主流的强化学习算法。为什么选择PPO，而不是其他更简单的策略梯度算法（如REINFORCE）或者Q-learning系算法？PPO中的KL散度惩罚项起到了什么关键作用？

   PPO 属于 on-policy 策略优化方法，更新更稳定，还能通过 clipping 和 KL 约束防止策略一下子跑偏

6. 如果在PPO训练过程中，KL散度惩罚项的系数 β 设置得过大或过小，分别会导致什么样的问题？你将如何通过实验和观察来调整这个超参数？

7. 什么是“奖励作弊/奖励黑客”（Reward Hacking）？请结合一个具体的LLM应用场景给出一个例子，并探讨几种可能的缓解策略。

   模型并没有真正学会我们想要的目标，而是学会了利用奖励函数、奖励模型或者评价机制中的漏洞，用一种“投机取巧”的方式拿到高分。

   模型可能学会一些“骗过奖励模型”的模式，而不是学到真正更好的行为。比如：

   - 回答变得过长
   - 过度迎合
   - 套模板化表述
   - 表面上更像高分答案，但实际质量下降

8. RLHF流程复杂且不稳定。近年来出现了一些替代方案，例如DPO。请解释DPO的核心思想，并比较它与传统RLHF（基于PPO）的主要区别和优势。

   通过一个配分函数Z，得到最优策略跟奖励函数之间的关系，然后通过BT偏好模型得到了DPO损失函数，

   DPO不使用奖励模型，拿偏好数据直接做带 reference 约束的对数概率的对比学习。

   对于一条数据 $(x, y_w, y_l)$，DPO 会比较：

   - 当前策略对 winner 和 loser 的对数概率差
   - 参考模型对 winner 和 loser 的对数概率差

   如果当前策略相对于参考模型，更倾向于 winner 而不是 loser，那么 loss 就会更小；反之就会被更新。

   * 优点：移除奖励模型训练和强化学习部分，实现端到端优化；直接使用分类损失，训练过程易于控制；内存占用更小
   * 缺点：仍然依赖高质量的偏好数据

9. 想象一下，你训练完成的RLHF模型在离线评估中表现优异，奖励模型分数很高，但上线后用户反馈其回答变得越来越“模式化”、奉承、且缺乏信息量。你认为可能的原因是什么？你会从哪些方面着手分析和解决这个问题？

   并不是训练没收敛，而是训练目标与真实产品目标没有完全一致。出现reward hacking

10. 你知道Deepseek的GRPO吗，**它和PPO的主要区别是什么**？优劣是什么？

   GRPO 是 DeepSeek 提出的 PPO 变体，核心思想是取消 PPO 里额外训练的 value/critic 模型，对同一个 prompt 一次采样多条回答，用这组回答的相对奖励经过组均值和组标准差归一化后，直接作为优势信号来更新 policy。相比 PPO，GRPO 最大的区别是不用单独学 value function，因此显存和训练开销更低；同时它更适合数学、代码这类有可验证奖励的推理任务。它的优势是简单、高效、资源占用低，

   

11. GSPO和DAPO有听说过吗？他们和GRPO有什么区别？

    把 GRPO 的**token-level** importance ratio，改成 **sequence-level**，GRPO 常见实现更偏 **token 级别目标**，而 GSPO 认为对于 LLM 这种“生成整段序列再给奖励”的场景，**以整条回答为优化单位更自然**。

    

12. 如何解决信用分配问题？token级别和seq级别的奖励有何不同？

    一段回答最终只拿到一个结果奖励时，怎么把这个奖励合理分配回前面的生成过程

    

13. 除了人类反馈，我们还可以利用AI自身的反馈来做对齐，即RLAIF。请谈谈你对RLAIF的理解，它的潜力和风险分别是什么？

14. trust-range和PPO的关系

    Trust Region 是"每步更新不要走太远"的核心思想，TRPO 用显式 KL 约束精确实现但计算代价极大；PPO 用 Clip 操作把概率比限制在 $[1-\epsilon, 1+\epsilon] $ 内，以一阶优化近似实现信任域效果，大幅降低了实现复杂度；在 RLHF 中两者叠加使用——Clip 约束单步更新幅度，KL 惩罚项约束整体不偏离参考模型

15. 为什么会有**importance sampling**，采样的策略模型和要训练的策略模型有偏差，重要性采样加以修正

    重要性采样的本质是**用概率比 $r_t = \pi_\theta / \pi_{old} $ 修正采样分布的偏差**，让旧策略采集的数据可以用来估计新策略的期望，从而实现数据复用；但新旧策略差距过大时概率比会极端导致训练不稳定，PPO 的 Clip 操作限制了这个修正幅度，在数据复用和训练稳定之间取得平衡。

16. PPO优势计算

    优势：比我预期的好多少 = 实际得到的收益 - 预期能得到的收益

    算 TD 残差 $\delta_t $： 就是"单步的优势估计"
    $$
    \delta_t = r_t + \gamma V(s_{t+1}) - V(s_t)
    $$
    用 GAE 把多步 $\delta $ 加权累加，从序列末尾往前算。得到优势

    

17. GRPO的损失计算，在序列级别的损失上，损失如何给到每一个token上?序列级别平均或批次级别平均

    GRPO 把序列级奖励标准化后得到序列级优势，将其广播到该序列每个 token 上，token 级概率比独立计算后与序列级优势相乘得到每个 token 的 loss；原始论文和 DeepSeek-R1 使用**序列级平均**（先按序列长度归一化再对 G 条序列平均），让每条回答平等贡献梯度，避免长序列主导训练。



# Agent

+++

1. 你如何定义一个基于 LLM 的智能体（Agent）？它通常由哪些核心组件构成？

   Agent = **LLM（推理核心、决策）+ 感知 + 记忆 + 工具 的自主决策系统**。它不只是被动地回答问题，而是能感知环境、制定计划、调用工具、持续行动，直到完成复杂目标

   LLM：推理核心。接受感知模块，参考记忆，与工具交互。常用ReAct范式驱动

   感知模块perception：处理多模态的输入（文本、图像、音频、结构化数据包括API返回的JSON和哦工具调用结果）

   记忆模块Memory：短期记忆、长期记忆、情景记忆

   工具调用Tool use：网络搜索、执行 Python 代码、调用外部 API、读写文件、操控浏览器等。LLM 通过生成结构化的 function call 来触发工具

   

2. 请详细解释 ReAct 框架。它是如何将思维链和行动结合起来，以完成复杂任务的？

   Thought → Action → Observation。让 LLM 在每一步都先"思考"再"行动"，并将行动结果作为新的观察融入下一轮推理

   思维链只推理不行动，知识静态、无法验证推理是否正确、容易幻觉。

   标准 CoT 的推理链完全在 LLM 内部，靠参数记忆支撑，一旦超出训练知识就会出错。ReAct 在每个 Thought 之后插入一个 Action→Observation 环节，用真实世界的信息校正推理链，让 Thought 始终有事实依据。

   

3. 在 Agent 的设计中，“规划能力”至关重要。请谈谈目前有哪些主流方法可以赋予 LLM 规划能力？（例如 CoT, ToT, GoT等）

   难点：对于任务拆解的合理性

   1. SFT中加入带有cot的推理数据集

   2. CoT让模型不直接生成答案，而是通过问题-推理链-答案，展开中间推理过程。有两种触发方式：Few-shot结合zero-shot

   3. ToT 是对 CoT 的重大扩展：把单条推理链变成一棵**搜索树**，在每个节点都探索多个可能的下一步，并用**评估函数**剪枝。

      ToT 的核心组件有四个：**思维生成器**（产生 k 个候选下一步）、**评估器**（给每个节点打分，可用 LLM 自评或外部启发式）、**搜索算法**（BFS 广度优先或 DFS 深度优先）、**剪枝策略**（丢弃低分节点）。适合解数学题、24点游戏、创意写作等需要回溯的任务，但调用 LLM 次数多，成本高。

      ToT 的思维只能父→子单向流动，GoT 打破了这一限制，允许节点**任意合并、聚合、形成环**，更接近人类真实的思考结构。

   4. GoT 引入了三类核心操作：**分解**（将大任务拆为并行子任务）、**聚合**（将多路思维合并为一个更好的结论）、**精炼**（对节点自我修正，形成有向环）。适合文档排序、集合运算等可并行化的任务，效率显著高于 ToT。

   

4. 感知模块：理解环境反馈（包括文本、图像、音频、带有JSON格式的工具返回等），难点在于如何处理多模态的信息对齐

   

5. Memory是 Agent 的一个关键模块。请问如何为 Agent 设计短期记忆和长期记忆系统？可以借助哪些外部工具或技术？

   难点是记忆检索效率的提升和记忆遗忘策略

   短期记忆：对应 Agent **当前会话/任务**中的信息存储，本质上是对 LLM **Context Window** 的管理。

   * 完整历史对话。信息无损，但容易超出上下文窗口限制
   * 滑动窗口。保留最近N轮对话，丢弃更早的历史。长度控制，但是丢失早期关键信息
   * 对话摘要压缩：定期对历史对话压缩成摘要，保留摘要+近N轮对话
   * 结构化状态：维护一个结构化的工作区，记录任务状态、中间变量等

   长期记忆：长期记忆对应 Agent **跨会话、跨任务**的知识积累，必须借助**外部存储**实现持久化。 通过RAG 按需取回

   * 语义记忆——知识库的形式：存储用户手册、产品文档、历史报告、背景资料，
   * 情节记忆——历史经历：存储 Agent 过去的完整对话记录、任务执行轨迹，供将来参考
   * 程序记忆——技能和经验：Agent的操作经验、任务模板、最佳实践。一般固化进 System prompt，通过FT让模型权重学会（工具）

   

6. Tool Use是扩展 Agent 能力的有效途径。请解释 LLM 是如何学会调用外部 API 或工具的？（可以从 Function Calling 的角度解释）

   **LLM 的本质是文本输入、文本输出。**

   **tool use本质是让 LLM 输出结构化的"意图描述"，**由外部系统解析并真正执行，再把结果还给 LLM

   * SFT：通过专门构造的训练数据学会的。训练数据包含：返回工具调用的格式、根据工具调用结果继续生成
   * RL：通过强化学习奖励"正确调用工具并给出好回答"的行为，惩罚"乱调工具"或"参数填错"的行为，提升工具调用的准确性

   Function的完整流程：

   * 开发者定义工具，用 json scheme 描述工具的名称、功能、参数

   * 工具描述注入prompt

   * 模型决策——调用 / 不调用：输出结构化的 tool call，模型停止生成

   * 外部系统执行工具

   * 结果回注，模型继续生成：工具执行结果作为新的消息追加到对话历史，模型基于结果生成回复

     

7. 请比较一下两个流行的 Agent 开发框架，如 LangChain 和 LlamaIndex。它们的核心应用场景有何不同？

8. 四个主要模块的联动逻辑：

   感知输入-决策规划-记忆检索-工具调用-结果返回-记忆更新

   

9. 在构建一个复杂的 Agent 时，你认为最主要的挑战是什么？

   * **任务的规划与分解的可靠性**：这是最根本的挑战。**LLM 在短任务上表现不错，但面对需要十几步甚至几十步的复杂任务时，会出现规划跑偏、中间推理步骤出错导致结果完全偏离**。这也是为什么 Plan-and-Execute、ReAct等框架被提出——本质上都是在试图让 Agent 的规划更稳定可控。
   * 工具调用的准确性和鲁棒性（工具选择错误、参数幻觉、调用失败处理错误信息、工具数量瓶颈）
   * 感知模块：多模态的输入的对齐
   * 记忆和状态管理：Agent 运行过程中需要维护大量状态
   * 安全控制

   构建复杂 Agent 最大的挑战不是单点能力，而是**如何在长链路、真实环境、不可预测输入下保持整体可靠性**；

   



# RAG

+++

1. 请解释 RAG 的工作原理。与直接对 LLM 进行微调相比，RAG 主要解决了什么问题？有哪些优势？
2. 一个完整的 RAG 流水线包含哪些关键步骤？请从数据准备到最终生成，详细描述整个过程。
3. 在构建知识库时，文本切块策略至关重要。你会如何选择合适的切块大小和重叠长度？这背后有什么权衡？
4. 如何选择一个合适的嵌入模型？评估一个 Embedding 模型的好坏有哪些指标？
5. 除了基础的向量检索，你还知道哪些可以提升 RAG 检索质量的技术？
6. 请解释“Lost in the Middle”问题。它描述了 RAG 中的什么现象？有什么方法可以缓解这个问题？
7. 如何全面地评估一个 RAG 系统的性能？请分别从检索和生成两个阶段提出评估指标。
8. 在什么场景下，你会选择使用图数据库或知识图谱来增强或替代传统的向量数据库检索？
9. 传统的 RAG 流程是“先检索后生成”，你是否了解一些更复杂的 RAG 范式，比如在生成过程中进行多次检索或自适应检索？
10. RAG 系统在实际部署中可能面临哪些挑战？
11. 了解搜索系统吗？和RAG有什么区别？
12. 知道或者使用过哪些开源RAG框架比如Ragflow？如何选择合适场景？





# 模型评估与Agent评估

+++

1. 为什么传统的 NLP 评估指标（如 BLEU, ROUGE）对于评估现代 LLM 的生成质量来说，存在很大的局限性？
2. 请介绍几个目前行业内广泛使用的 LLM 综合性基准测试，并说明它们各自的侧重点。（例如：MMLU, Big-Bench, HumanEval）
3. 什么是“LLM-as-a-Judge”？使用 LLM 来评估另一个 LLM 的输出，有哪些优点和潜在的偏见？
4. 如何设计一个评估方案来衡量 LLM 的特定能力，比如“事实性/幻觉水平”、“推理能力”或“安全性”？
5. 评估一个 Agent 为什么比评估一个基础 LLM 更加困难和复杂？评估的维度有哪些不同？
6. 你了解哪些专门用于评估 Agent 能力的基准测试？这些基准通常如何构建测试环境和任务？
7. 在评估一个 Agent 的任务完成情况时，除了最终结果的正确性，还有哪些过程指标是值得关注的？（例如：效率、成本、鲁棒性）
8. 什么是红队测试？它在发现 LLM 和 Agent 的安全漏洞与偏见方面扮演着什么角色？
9. 在进行人工评估时，如何设计合理的评估准则和流程，以保证评估结果的客观性和一致性？
10. 如何持续监控和评估一个已经部署上线的 LLM 应用或 Agent 服务的表现，以应对可能出现的性能衰退或行为漂移？

