# 计算机基础

+++

## 操作系统

+++

### 进程与线程的区别

* **进程（Process）** 是操作系统分配资源的基本单位，每个进程有独立的内存空间（代码段、数据段、堆、栈）、文件描述符等。进程之间互相隔离，一个进程崩溃不会影响另一个。
* **线程（Thread）** 是 CPU 调度的基本单位，是进程内的执行流。同一进程的多个线程共享内存空间和文件描述符，但各自有独立的栈和寄存器状态。

| 维度     | 进程                          | 线程             |
| -------- | ----------------------------- | ---------------- |
| 内存空间 | 独立                          | 共享（同进程内） |
| 创建开销 | 大（需复制内存映射）          | 小               |
| 通信方式 | IPC（管道、共享内存、socket） | 直接读写共享变量 |
| 崩溃影响 | 不影响其他进程                | 可能拖垮整个进程 |
| 切换开销 | 大（需切换页表等）            | 小               |

### 进程切换和线程切换哪个开销更大

进程大

进程有独立地址空间，切换进程时往往要切换页表、虚拟内存映射。进程切换涉及更多内核资源

线程切换：同一进程内线程共享地址空间，不需要切换整个内存映射，主要切换的是寄存器、栈、程序计数器等线程上下文

### 并发和并行有什么区别

**并发**：一段时间内同时处理多个任务，看起来像同时进行。
**并行**：同一时刻真正同时执行多个任务。

### 死锁产生的四个必要条件

**互斥**：资源一次只能被一个线程/进程占有

**请求并保持**：已经拿到部分资源，还继续申请新资源

**不可剥夺**：资源不能被强行抢走

**循环等待**：多个进程形成资源等待环

### 用户态和内核态

操作系统把 CPU 的执行权限分成两级：

用户态：

- 权限低
- 应用程序运行在这里
- 不能直接访问硬件、不能执行特权指令

内核态：

- 权限高
- 操作系统内核运行在这里
- 可以访问硬件、管理内存、调度进程等

### 一次系统调用大概发生了什么？

系统调用就是：**用户程序请求操作系统帮它做一件只有内核能做的事**。

大概过程：

1. 程序在**用户态**调用库函数，比如 `read()`
2. 执行特殊指令，触发**陷入内核**
3. CPU 从用户态切换到**内核态**
4. 根据系统调用号找到对应内核服务程序
5. 内核执行对应操作，比如读文件、分配内存
6. 执行完成后返回结果
7. CPU 再切回**用户态**

### 虚拟内存是什么？分页和分段的区别是什么

虚拟内存是操作系统给每个进程提供的一个**连续、独立的地址空间**，实际的物理内存映射由操作系统管理

分页：把虚拟内存和物理内存都分成固定大小的块。方便管理，没有外部碎片

分段：按逻辑结构划分：代码段、数据段、栈段 等。容易产生外部碎片

### 堆和栈有什么区别？

栈

- 编译器自动分配和释放
- 存放函数调用信息、局部变量、返回地址等
- 速度快
- 空间通常较小

堆

- 程序员手动申请释放，或者由运行时管理
- 适合动态分配大块内存
- 生命周期更灵活
- 管理开销更大，速度相对慢

关键区别：

- **栈**：自动管理，连续，高效
- **堆**：动态管理，灵活，但容易碎片化和泄漏



## 计算机网络

+++

### TCP是什么

* TCP 是**传输控制协议**（Transmission Control Protocol），是互联网中最核心的传输层协议之一。

* 特性：

  1. **可靠传输**：数据包丢了会重传，乱序了会重排，保证对方收到的数据完整且有序。
  2. **面向连接**：通信前必须先建立连接（三次握手），结束后释放连接（四次挥手）。
  3. **流量控制**：通过滑动窗口机制，防止发送方发太快把接收方"淹没"。
  4. **拥塞控制**：感知网络拥堵时主动降速，避免把网络"打爆"。

  |          | TCP                 | UDP                 |
  | -------- | ------------------- | ------------------- |
  | 可靠性   | 有保证              | 不保证              |
  | 连接     | 需要建立            | 无连接              |
  | 速度     | 较慢                | 快                  |
  | 适用场景 | 网页、文件传输、SSH | 视频直播、DNS、游戏 |

### TCP 三次握手与四次挥手，为何挥手需要四次

* TCP（Transmission Control Protocol，传输控制协议）是一种**面向连接**的、**可靠的**传输层协议

* 它的核心特点是：保证数据按序到达、不丢失、不重复。我们平时用的 HTTP、SSH 等协议底层都跑在 TCP 上。

* 三次握手：

  目的是让双方确认彼此的收发能力都正常，并同步初始序列号。

  **第一次：**客户端发送 **SYN 包**，携带一个初始序列号（比如 seq=x），表示"我想建立连接"。此时客户端进入 SYN_SENT 状态。

  **第二次：**服务端收到后，**回复 SYN+ACK 包**，确认客户端的序列号（ack=x+1），同时带上自己的初始序列号（seq=y），表示"我同意，我也准备好了"。服务端进入 SYN_RCVD 状态。

  **第三次：**客户端收到后，**发送 ACK 包**（ack=y+1），表示"我确认收到了"。双方进入 ESTABLISHED 状态，连接建立完成。

* 四次挥手（断开连接）

  因为 TCP 是全双工的，每个方向的连接需要单独关闭。

  第一次：主动关闭方（假设是客户端）发送 FIN，表示"我不再发数据了"。进入 FIN_WAIT_1 状态。

  第二次：服务端收到 FIN，回复 ACK，表示"我知道了"。服务端进入 CLOSE_WAIT 状态，客户端进入 FIN_WAIT_2 状态。此时连接处于半关闭状态——客户端不发了，但服务端还可以继续发。

  第三次：服务端数据发完后，也发送 FIN，表示"我也不发了"。服务端进入 LAST_ACK 状态。

  第四次：客户端收到 FIN，回复 ACK。客户端进入 TIME_WAIT 状态，等待 2MSL（报文最大生存时间的两倍）后才真正关闭，目的是确保最后这个 ACK 能到达服务端，如果丢了服务端会重发 FIN，客户端还能再确认。

  

  

### HTTP/1.1、HTTP/2、HTTP/3 的主要区别

### 讲讲 HTTPS 的握手过程，TLS 1.3 做了哪些优化

### DNS 解析的完整流程是什么

### TCP 的拥塞控制算法有哪些阶段
