# 训练

+++

1. **预训练损失函数**

2. **SFT 损失函数**：

   + token 级别的交叉熵

   $$
   \mathcal{L_{SFT}} = -\frac{1}{N} \sum_{t=1}^{N} \log P(x_t \mid x_{<t})
   $$

3. **PPO 损失函数**

   

4. **DPO 损失函数**

5. **GRPO 损失函数**
   $$
   \mathcal{L}_{\text{GRPO}} = -\frac{1}{G} \sum_{i=1}^{G} \frac{1}{|y_i|} \sum_{t=1}^{|y_i|} \left[ \min\left(\rho_{i,t} \hat{A}_i,\ \text{clip}(\rho_{i,t}, 1-\varepsilon, 1+\varepsilon) \cdot \hat{A}_i \right) - \beta \cdot \underbrace{\log \frac{\pi_\theta(a_{i,t}|s_{i,t})}{\pi_{\text{ref}}(a_{i,t}|s_{i,t})}}_{\text{KL penalty per token}} \right]
   $$
   流程：

   1. 组内采样：对同一个 prompt，用旧策略一次性采样G条响应
      $$
      \{y_1, y_2, \dots, y_G\} \sim \pi_{\theta_{old}}(\cdot | x)
      $$

   2. 计算每条响应的 Reward（序列级的 Reward）

   3. 组内对G条响应得分进行标准归一化得到奖励A，并广播到每一个 token
      $$
      \hat{A}_i = \frac{r_i - \text{mean}(\{r_1,\dots,r_G\})}{\text{std}(\{r_1,\dots,r_G\})}
      $$

   4. 计算Loss，采用 Clip 限制策略更新到幅度，并加入逐个 token 到KL惩罚

6. **DAPO 损失函数** （Dynamic sAmpling Policy Optimization）

   * 目标函数为：

   $$
   \mathcal{J}_\text{DAPO}(\theta) = \mathbb{E} \left[ \frac{1}{\sum_{i=1}^{G}|o_i|} \sum_{i=1}^{G} \sum_{t=1}^{|o_i|} \min\left(r_{i,t}(\theta)\hat{A}_{i,t},\ \text{clip}(r_{i,t}(\theta), 1-\varepsilon_\text{low}, 1+\varepsilon_\text{high})\hat{A}_{i,t}\right) \right]
   $$

   ​	约束条件：$0 < |\{o_i \mid \text{is\_equivalent}(a, o_i)\}| < G $ 

   * 相较于GRPO的改动：

     1. 移除了 KL 散度：Long-CoT 训练中策略分布会大幅偏离初始模型，KL 约束反而限制了学习能力。

     2. Clip-Higher：解耦裁剪范围。对称裁剪会压制低概率 token 的概率提升空间，导致熵崩塌。上限放宽，给探索 token 更大的概率增长空间，同时下限不变避免空间崩塌

     3. Dynamic Sampling（动态采样）：当某个 prompt 组内所有回答全对（acc=1）或全错（acc=0）时，组内奖励方差=0，优势=0，如果一个batch中全是这样的样本，梯度为零。随着训练进行，一个batch内 acc=1 的比例持续上升，梯度信号减弱，批次噪声增大。因此，DAPO 要求每一个 batch 中过采样直到填满 N 个有效 prompt

     4. Token-Level Loss（token 级损失）：GRPO 以 组内样本 为单位归一化，每条回答贡献相等权重。长回答中每个 token 的实际梯度贡献被稀释（先是序列内归一稀释了长度贡献）——优质的长思维链无法被充分强化，垃圾的长回答也无法被充分惩罚。修改后，先对批次内所有 token 的 Loss 求和再在组内所有token进行归一化，长回答天然获得更多权重，长思维链中的模式能被充分强化或抑制

     5. Overlong Reward Shaping（奖励塑形）：GRPO 被截断的过长回答直接给负向奖励，会让模型困惑：明明推理过程正确，只是太长就被惩罚。这引入奖励噪声导致训练不稳定。DAPO 提出了Soft Overlong Punishment在缓冲区内线性惩罚，越长扣越多，超出上限才给 -1，奖励信号平滑

        

7. **GSPO 损失函数**

   + GRPO 这类方法通常按 token 去算 importance ratio；但 GSPO 认为这样会引入更大的训练噪声，因为每个 token 只被采样一次，token 级修正不但难以真正完成分布校正，还可能带来高方差，导致梯度不稳定，严重时甚至训练崩掉

   + 计算流程：

     1. 在一个 prompt 下采样一组回答，然后对每个回答计算一个**序列级的重要性比值**：把整条序列的 likelihood ratio 取出来，再做一个按长度归一化的处理（整条response所有 token 的 log-ratio 求平均，再 取指数 回去）
        $$
        s_i(\theta)=\left(\frac{\pi_\theta(y_i|x)}{\pi_{\theta_{\mathrm{old}}}(y_i|x)}\right)^{1/|y_i|} \\=
        \exp\left(
        \frac{1}{|y_i|}
        \sum_{t=1}^{|y_i|}
        \log
        \frac{\pi_\theta(y_{i,t}\mid x,y_{i,<t})}
        {\pi_{\theta_{\text{old}}}(y_{i,t}\mid x,y_{i,<t})}
        \right)
        $$

     2. Loss函数：
        $$
        \mathcal L_{\text{GSPO}}(\theta)
        = -
        \mathbb E\left[
        \frac{1}{G}\sum_{i=1}^{G}
        \min\Big(
        s_i(\theta)\hat A_i,\;
        \text{clip}(s_i(\theta),1-\varepsilon,1+\varepsilon)\hat A_i
        \Big)
        \right]
        $$

     3. 相较于 GRPO 的优势：

        1. 训练稳定
        2. 更高效：在相同训练成本下有更好的表现
        3. 更适合 MoE 架构：GRPO 做 MoE 强化学习时，常常需要一个额外的 **Routing Replay** 策略，去**缓存旧策略激活过的专家路由**，否则训练很难正常收敛。但 GSPO 因为只关注 sequence likelihood，对专家激活波动没那么敏感。

8. 模型蒸馏的两种策略的 Loss 计算 / 训练过程

   1. off-policy：STF

   2. on-policy：
      $$
      \mathcal{L}_{KD} = \tau^2 \cdot \sum_{t=1}^{N} D_{KL}\left( P_T^{(\tau)}(\cdot \mid x_{<t}) \,\|\, P_S^{(\tau)}(\cdot \mid x_{<t}) \right) \\= \frac{1}{N} \sum_{t=1}^{N} \sum_{k} P_T^{(\tau)}(k \mid x_{<t}) \log \frac{P_T^{(\tau)}(k \mid x_{<t})}{P_S^{(\tau)}(k \mid x_{<t})}
      $$
      实际训练时的混合损失包含硬标签的监督信号，通常和 SFT 的交叉熵混合：
      $$
      \mathcal{L} = \alpha \cdot \mathcal{L}_{SFT} + (1 - \alpha) \cdot \mathcal{L}_{KD}
      $$

9. 训练过程中的难点/基本情况的应对：

   1. Clip 机制做了改进。传统方法对重要性采样比率的上下限使用对称裁剪。DAPO 发现训练初期会出现**熵崩塌（entropy collapse）**现象

10. 灾难性遗忘（Catastrophic Forgetting）

    * 现象：模型在目标任务上提升了，但在其他通用你任务上能力严重退化。
    * 原因：
      1. 神经网络参数共享，优化一个下游目标时必须改变能力相关的参数；
      2. 如果微调数据的分布与预训练数据差异大，梯度更新会把参数拉向新分布，远离旧分布
    * 解决方案：
      1. 数据混合（Data Mixing）：在SFT数据中混入一定比例（如5%~20%）的预训练数据
      2. 参数高效微调（PEFT）：LoRA / QLoRA只更新少量参数（低秩矩阵），预训练权重冻结不动，从根本上限制遗忘。
      3. 较小的学习率：SFT通常用1e-5 ~ 5e-6，远小于预训练的学习率。RL阶段更小，如1e-6~5e-7。
      4. 评估：训练过程中不只评估目标任务，也定期评估通用 benchmark

11. SFT 过程中出现的过拟合

    * 现象：训练loss持续下降，但验证集loss开始上升；生成内容开始复读训练数据中的特定句式
    * 原因：
      1. 参数与数据的比例严重失衡
      2. 数据没去重或者去重不彻底：同一个或者高度相似的样本多次出现
    * 解决方案：
      1. 控制epoch：高质量数据通常只需1~3个epoch
      2. 严格数据去重：用 n-gram overlap、embedding相似度等方法做去重。
      3. Dropout：虽然预训练时通常关闭dropout，但SFT阶段可以开启（如0.05~0.1）

12. Reward Hacking

    * 现象：reward曲线持续上升，但人工评估质量反而下降或停滞。模型学会了"讨好"reward model获取高分数而非真正变好
    * 原因：也是用有限数据训练出来的一个model， 对真实人类偏好拟合的不完美。对于较长、格式上更好、肯定回复等回答会给出高分
    * 解决方法：
      1. KL Penalty：约束 policy model 不要偏离 reference model 太远。通常用 adaptive KL controller：设定KL目标值（如6.0），KL高于目标就增大β，低于就减小。
      2. Reward Clipping / Reward Normalization：对 reward 值做截断或者归一化，防止极端 reward 值主导梯度更新
      3. Reward Model Ensemble：训练多个 RM 取平均
      4. 对于一些可以验证的维度，引入可验证的函数函数，不完全依赖于 learned RM

13. Entropy Collapse（熵坍缩）

    * 现象：模型输出的token概率分布越来越集中，entropy持续下降趋近于0，生成内容高度重复、缺乏多样性。
    * 原因：
      1. PPO中，当reward model对某类回答一致性地给高分时，策略梯度会不断强化这些token的概率，同时压低其他token。这形成一个正反馈循环——高概率token获得更多采样机会，进而被进一步强化。分布必然坍缩
      2. GRPO中，当group内采样已经高度同质化时，排序信号本身就带有噪声，可能进一步加速collapse（DAPO中利用动态采样规避了这个问题）
    * 解决方法：可以借鉴一下DAPO中的做法（动态采样，Clip-Higher）

14. GRPO 中对比信号弱（Loss几乎不变）

    * 现象：训练loss几乎不变
    * 原因：组内采样后G个回答质量接近，reward 方差小，带入了噪声，梯度接近0
    * 解决方法：增大 Group size；使用更高的采样 temperature；过滤简单的 prompt